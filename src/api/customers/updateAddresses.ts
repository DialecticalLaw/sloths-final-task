// import type { Customer } from '@commercetools/platform-sdk';
// import type { AddressActionData, UpdatePasswordData } from '../api.interfaces';
// import { apiRoot } from '../apiRoot';

// export async function deleteAddress({addressId, customerId}: AddressActionData) {
//   try {
//     const customer = (
//       await apiRoot
//         .customers()
//         .withId({ ID: customerId })
//         .post({ body: { actions: , version: customerData.version } })
//         .execute()
//     ).body;
//     return customer;
//   } catch (error) {
//     console.error(error);
//     throw error;
//   }
// }
